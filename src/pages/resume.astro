---
import Layout from '../layouts/Layout.astro';
import Icon from '../components/Icon/Icon.astro';

interface ExperienceItem {
  company: string;
  logo?: string;
  title: string;
  employmentType: string;
  startDate: string;
  endDate?: string;
  duration: string;
  location: string;
  context?: string;
  bullets?: string[];
  governance?: string[];
  isCurrent?: boolean;
  website?: string;
}

const experiences: ExperienceItem[] = [
  {
    company: "A-Frame Solutions",
    title: "AI Product Manager · GTM",
    employmentType: "Contract",
    startDate: "Mar 2025",
    endDate: "Jun 2026",
    duration: "1 yr 4 mos",
    location: "Arlington, VA",
    context: "ArcSuiteAI (@@$5.4M ARR@@) — AI procurement-lifecycle SaaS for federal agencies",
    bullets: [
      "Owned the ArcSuiteAI product line (ArcCompose, ArcInsight, ArcTracker, ArcSelect, ArcPropose) — roadmap, messaging architecture, design system, and content strategy.",
      "Ran daily scrums and product backlog for an **11-person Agile pod** through **FedRAMP Moderate** and **IRS ATO**.",
      "Drove AI strategy: moved developers to multi-terminal TUI agents (OpenCode, Claude Code) alongside Cursor; researched prompt engineering and RAG — sharply increased dev throughput.",
      "Shipped **29 PRs** in TypeScript using agentic tooling; Prisma → Postgres → AWS GovCloud.",
      "Grew LinkedIn @@16×@@ in two weeks (12 → 200) with product video walkthroughs produced in video-editing tools — validated B2G positioning vs. competitors.",
      "Drove @@$95K (38%)@@ in cost savings with a FedRAMP partner via willingness-to-pay analysis; built the VC outreach strategy for investor engagement.",
    ],
    governance: ["FedRAMP Moderate", "IRS ATO"],
    isCurrent: false
  },
  {
    company: "Disca",
    title: "Fractional CTO · Product Owner",
    employmentType: "Contract",
    startDate: "Feb 2026",
    duration: "Present",
    location: "Remote / Phoenix, AZ",
    context: "Clinical Trial Management",
    bullets: [
      "Own product vision, roadmap, and technical architecture for a Clinical Trials Data Platform.",
      "Lead 3 developers and 2 SMEs; run owner demos that translate features into business value.",
      "Deliver executive strategy across growth, operations, product, and marketing vectors.",
    ],
    isCurrent: true
  },
  {
    company: "Nodetus Integrators LLC",
    title: "Founder & Managing Director",
    employmentType: "Self-employed",
    startDate: "Jan 2017",
    endDate: "Present",
    duration: "9+ yrs",
    location: "Arlington, VA",
    context: "AI services, federal B2B",
    bullets: [
      "Own product and delivery end-to-end across six federal subcontracts: **$1.1M+** cumulative revenue, @@40%@@ YoY growth, @@90%@@ renewal over 8+ years; 2.5 FTE peak.",
      "Clients: Disca, Sprezzatura, A-Frame, MAXIMUS, IME, AFI — delivered to HHS, NIH, Commerce, IRS.",
      "Active in ACT-IAC (Acquisitions & Project Management COIs).",
    ],
    isCurrent: true
  },
  {
    company: "Tria Federal",
    title: "Sr. Technical Project Manager · Product Owner",
    employmentType: "Full-time",
    startDate: "2020",
    endDate: "2025",
    duration: "5 yrs",
    location: "Arlington, VA",
    context: "VA EHR Modernization (multi-vendor)",
    bullets: [
      "Owned delivery of **15+ EHR interfaces** as products, taking them LIVE at VA hospitals on a multi-vendor program.",
      "Authored Scrum Master playbooks adopted program-wide across **20+ interface teams (400+ people)**.",
      "Productized project setup: built a Python + Excel utility that cut new-interface setup from days to minutes — est. @@$160K/yr@@ in cross-team labor saved.",
      "Modernized legacy MUMPS to Cerner/Oracle via FHIR/HL7.",
    ],
    isCurrent: false
  },
  {
    company: "MAXIMUS",
    title: "Technical Agile Product Delivery Lead",
    employmentType: "Full-time",
    startDate: "2019",
    endDate: "2020",
    duration: "1 yr 1 mo",
    location: "Vienna, VA",
    context: "$1B+ GovTech & services",
    bullets: [
      "Owned and solo-built the NIH Help Desk online certification product during COVID — process, testing, and video — driving repeat selection across **6 NIH OAMP subcontracts** (2019–2024).",
      "PM for Genesys FedRAMP across a **50-person team** (security, ops, data warehouse, on-prem, AWS engineers, platform solutions architects).",
      "Built Agile/digital playbooks as reusable products with the CTO, VP of Technology, and VP of Agile Services; led orals and proposal solutioning.",
    ],
    governance: ["FedRAMP"],
    isCurrent: false
  },
  {
    company: "RWD Consulting LLC",
    title: "Product Owner · Proposal & BD Platform",
    employmentType: "Full-time",
    startDate: "2018",
    endDate: "2019",
    duration: "1 yr 2 mos",
    location: "Washington, DC",
    context: "8(a) services",
    bullets: [
      "Owned the proposal product end-to-end: **14 wins** and @@165%@@ revenue growth ($8M → $21M) across 30 proposals.",
      "Built and owned a SharePoint + Flow BD platform for opportunity management; @@90%@@ user adoption.",
      "Managed 6 FTEs (4 developers, 2 support) at DHS TSA, Crystal City, VA.",
    ],
    isCurrent: false
  }
];

const earlierExperience: { company: string; title: string; context?: string; dates: string }[] = [
  { company: "Highlight Technologies", title: "Product Owner · Proposals", dates: "2016 — 2017" },
  { company: "Quick Services", title: "Proposal Manager", context: "SDVOSB / DoD", dates: "2015 — 2016" },
  { company: "DRT Strategies", title: "Proposal Manager", dates: "2014 — 2015" },
  { company: "CapRelo", title: "Proposal Coordinator", dates: "2013 — 2014" },
];

const coreCompetencies = [
  "AI-Native Product Strategy",
  "Agentic System Building",
  "FedRAMP & ATO",
  "Agile / Scrum",
  "DevSecOps",
  "Health Informatics",
  "Product Design",
  "Product Marketing",
  "Vendor Negotiation",
  "GTM",
  "Business Development",
  "Capture & Proposal Mgmt",
  "Project Management",
  "Executive Strategy",
];

const techStack = [
  "Python",
  "Rust & WASM",
  "Prisma",
  "Drizzle",
  "Graphile",
  "Postgres",
  "Neo4j",
  "Supabase",
  "GCP",
  "AWS",
  "Cloudflare",
  "FHIR",
  "HL7",
  "Astro",
  "Vue",
  "Solid.js",
  "Next.js",
  "Nuxt.js",
  "Anthropic",
  "OpenAI",
  "AI-SDK",
  "Cursor",
  "Warp Terminal",
  "OpenCode",
  "Git",
  "Local-First",
  "Fedora",
  "Arch Linux",
  "Ubuntu",
  "Pop!_OS",
  "Firecrawl",
  "Apollo",
  "HubSpot",
  "Twilio",
  "SendGrid",
];

const education = [
  {
    school: "George Mason University",
    degree: "M.S., Technology Management",
    detail: "Apr 2020 · 3.74 GPA",
  },
  {
    school: "Longwood University",
    degree: "B.A., English",
    detail: "Apr 2012",
  }
];

const certifications: { name: string; note?: string }[] = [
  { name: "AWS Certified Cloud Practitioner" },
  { name: "CompTIA Security+" },
  { name: "Product School — PMC" },
  { name: "Nielsen Norman — UX Master (UXMC)", note: "~2,000 globally" },
  { name: "SAFe 4 Practitioner" },
  { name: "ITIL v4 Foundation" },
];

const specializations = [
  { title: "Digital Healthcare Informatics & AI", org: "Illinois Tech" },
  { title: "AI Agents with Model Context Protocol", org: "Vanderbilt University" },
  { title: "Supervised Machine Learning", org: "DeepLearning.AI" },
  { title: "Algorithms & Data Structures", org: "UC San Diego" },
  { title: "Python for Everybody", org: "University of Michigan" },
  { title: "PostgreSQL for Everybody", org: "University of Michigan" },
  { title: "Introduction to Computer Programming", org: "University of London" },
  { title: "Computer Communications", org: "University of Colorado" },
  { title: "Finance & Quantitative Modeling", org: "University of Pennsylvania" },
  { title: "Precalculus through Data & Modeling", org: "Johns Hopkins University" },
  { title: "Digital Marketing", org: "University of Illinois" },
  { title: "Search Engine Optimization (SEO)", org: "UC Davis" },
  { title: "RPA Specialization", org: "UiPath" },
  { title: "Pre-Security Learning Path", org: "TryHackMe" },
];

const contactItems = [
  { label: "erikk@shupp.dev", href: "mailto:erikk@shupp.dev" },
  { label: "703-618-4953", href: "tel:+17036184953" },
  { label: "shupp.dev", href: "https://www.shupp.dev" },
  { label: "in/shupp-erikk", href: "https://www.linkedin.com/in/shupp-erikk" },
  { label: "Falls Church, VA" },
];

// Inline emphasis: **bold** -> <strong>, @@metric@@ -> amber span
const renderEmphasis = (s: string) =>
  s
    .replace(/@@(.+?)@@/g, '<span class="metric">$1</span>')
    .replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
---

<Layout title="Resume - Erikk Shupp" description="Professional resume of Erikk Shupp - Sr. Technical Product Manager">
  <div class="resume-sheet">
    <div class="sheet-bar"></div>

    <div class="resume-shell">
      <a href="/about" class="back-link">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M19 12H5M12 19l-7-7 7-7"/>
        </svg>
        Back to About
      </a>

      <header class="resume-header">
        <h1 class="name">Erikk Shupp<span class="dot">.</span></h1>
        <p class="role">Sr. Technical Product Manager</p>
        <p class="tagline">Product · Design · Development · Marketing · Growth</p>

        <div class="contact-row">
          {contactItems.map((item) => (
            <span class="contact-item">
              <span class="diamond">◆</span>
              {item.href ? (
                <a href={item.href} target={item.href.startsWith('http') ? '_blank' : undefined} rel="noopener noreferrer">{item.label}</a>
              ) : (
                <span>{item.label}</span>
              )}
            </span>
          ))}
        </div>

        <p class="summary" set:html={renderEmphasis(
          "**10+ years** launching production-ready software products across commercial, government, and medical systems. Across my career I've managed @@~184 developers@@ over @@22 teams@@ at the VA, NIH, Treasury, and DHS, plus small-to-mid-size software companies. Deep in high-complexity DevSecOps environments for **ATO**, **FedRAMP**, and **SOC 2** compliance. Committed @@500+ PRs@@ in 2026 alone across TypeScript, Python, and Rust. A complete technical product manager: GTM strategy, product and project management, design &amp; branding, and hands-on engineering."
        )} />
      </header>

      <div class="resume-grid">
        <main class="resume-main">
          <section>
            <div class="sec-head"><h2>Relevant Experience</h2></div>

            {experiences.map((job, i) => (
              <article class="exp-item">
                <div class={`exp-logo ${i % 2 === 0 ? 'teal' : 'dark'}`}>
                  <span>{job.company.charAt(0)}</span>
                </div>

                <div class="exp-body">
                  <div class="exp-top">
                    <h3 class="exp-title">{job.title}</h3>
                    <span class="exp-dates">{job.startDate} — {job.endDate || "Present"}</span>
                  </div>
                  <p class="exp-meta">
                    {job.company}
                    <span class="mid-dot">·</span>{job.employmentType}
                    <span class="mid-dot">·</span>{job.location}
                  </p>
                  {job.context && (
                    <p class="exp-context" set:html={renderEmphasis(job.context)} />
                  )}

                  {job.bullets && (
                    <ul class="exp-bullets">
                      {job.bullets.map((b) => (
                        <li set:html={renderEmphasis(b)} />
                      ))}
                    </ul>
                  )}

                  {job.governance && (
                    <p class="exp-gov">
                      <span class="gov-label">governance</span>
                      {job.governance.map((g) => (
                        <span class="gov-chip">{g}</span>
                      ))}
                    </p>
                  )}
                </div>
              </article>
            ))}
          </section>

          <section class="other-exp">
            <div class="sec-head"><h2>Earlier Experience</h2></div>
            {earlierExperience.map((item) => (
              <article class="exp-item compact">
                <div class="exp-logo dark"><span>{item.company.charAt(0)}</span></div>
                <div class="exp-body">
                  <h3 class="exp-title small">{item.title}</h3>
                  <p class="exp-meta">
                    {item.company}
                    {item.context && (<><span class="mid-dot">·</span>{item.context}</>)}
                    <span class="mid-dot">·</span>{item.dates}
                  </p>
                </div>
              </article>
            ))}
          </section>
        </main>

        <aside class="resume-aside">
          <section class="aside-block">
            <h4 class="aside-head">Core</h4>
            <div class="pill-cloud">
              {coreCompetencies.map((c) => (
                <span class="pill core">{c}</span>
              ))}
            </div>
          </section>

          <section class="aside-block">
            <h4 class="aside-head">Tech</h4>
            <div class="pill-cloud">
              {techStack.map((t) => (
                <span class="pill">{t}</span>
              ))}
            </div>
          </section>

          <section class="aside-block">
            <h4 class="aside-head">Education</h4>
            {education.map((edu) => (
              <div class="edu-item">
                <p class="edu-degree">{edu.degree}</p>
                <p class="edu-meta">{edu.school}</p>
                <p class="edu-meta light">{edu.detail}</p>
              </div>
            ))}
          </section>

          <section class="aside-block">
            <h4 class="aside-head">Certifications</h4>
            <ul class="cert-list">
              {certifications.map((cert) => (
                <li>
                  <span class="diamond">◆</span>
                  <span class="cert-body">
                    {cert.name}
                    {cert.note && <span class="cert-note">{cert.note}</span>}
                  </span>
                </li>
              ))}
            </ul>
          </section>
        </aside>
      </div>

      <section class="spec-section">
        <div class="sec-head"><h2>Specializations</h2></div>
        <div class="spec-grid">
          {specializations.map((s) => (
            <div class="spec-card">
              <p class="spec-title">{s.title}</p>
              <p class="spec-org">{s.org}</p>
            </div>
          ))}
        </div>
      </section>

      <footer class="resume-footer">
        <div class="resume-actions">
          <button class="btn btn-primary" onclick="window.print()">
            <Icon name="Printer" size={18} weight="duotone" />
            Print
          </button>
          <a href="/api/resume.pdf" class="btn btn-secondary">
            <Icon name="Download" size={18} weight="duotone" />
            Download PDF
          </a>
        </div>
      </footer>
    </div>
  </div>
</Layout>

<style>
  /* Load editorial typefaces matching the PDF (slab name + serif body + mono labels) */
  @import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;700&family=Roboto+Slab:wght@600;700;800&family=Source+Serif+4:ital,opsz,wght@0,8..60,400;0,8..60,600;0,8..60,700;1,8..60,400&display=swap');

  .resume-sheet {
    /* Resume-scoped palette (warm, editorial) */
    --r-bg: #f5f3ec;
    --r-surface: #faf8f2;
    --r-ink: #262420;
    --r-muted: #6f6d65;
    --r-teal: #5f7d80;
    --r-teal-soft: rgba(95, 125, 128, 0.10);
    --r-teal-line: rgba(95, 125, 128, 0.28);
    --r-amber: #bd6f31;
    --r-border: #ddd8cb;
    --r-dash: rgba(95, 125, 128, 0.22);

    --r-serif: 'Source Serif 4', Georgia, 'Times New Roman', serif;
    --r-slab: 'Roboto Slab', 'Source Serif 4', Georgia, serif;
    --r-mono: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace;

    background: var(--r-bg);
    color: var(--r-ink);
    min-height: 100vh;
    /* Break out of the Layout's centered, 960px-capped <main> so the
       resume spans the full window to the right of the side nav. */
    width: calc(100vw - 240px);
    margin: -2rem 0 -2rem -2rem;
    padding: 0;
    overflow-x: hidden;
  }

  html.dark .resume-sheet {
    --r-bg: #1b1a17;
    --r-surface: #232220;
    --r-ink: #e9e6df;
    --r-muted: #a29f96;
    --r-teal: #8fb1b3;
    --r-teal-soft: rgba(143, 177, 179, 0.10);
    --r-teal-line: rgba(143, 177, 179, 0.28);
    --r-amber: #d9985a;
    --r-border: #353330;
    --r-dash: rgba(143, 177, 179, 0.22);
  }

  .sheet-bar {
    height: 6px;
    background: linear-gradient(90deg, var(--r-teal), color-mix(in srgb, var(--r-teal) 55%, #34484a));
  }

  .resume-shell {
    max-width: 1500px;
    margin: 0 auto;
    padding: 2rem clamp(2rem, 4vw, 5rem) 3.5rem;
  }

  /* Back Link */
  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--r-muted);
    text-decoration: none;
    font-family: var(--r-mono);
    font-size: 0.78rem;
    letter-spacing: 0.04em;
    margin-bottom: 2rem;
    transition: color 0.2s ease, transform 0.2s ease;
  }
  .back-link:hover { color: var(--r-teal); }
  .back-link:hover svg { transform: translateX(-3px); }
  .back-link svg { transition: transform 0.2s ease; }

  /* Header */
  .resume-header { margin-bottom: 2.25rem; }

  .name {
    font-family: var(--r-slab);
    font-weight: 800;
    font-size: clamp(2.6rem, 6vw, 4rem);
    line-height: 1;
    letter-spacing: -0.02em;
    color: var(--r-ink);
    margin: 0 0 0.5rem;
  }
  .name .dot { color: var(--r-teal); }

  .role {
    font-family: var(--r-mono);
    text-transform: uppercase;
    letter-spacing: 0.22em;
    font-size: 0.92rem;
    font-weight: 500;
    color: var(--r-teal);
    margin: 0 0 0.5rem;
  }

  .tagline {
    font-family: var(--r-mono);
    font-size: 0.82rem;
    letter-spacing: 0.02em;
    color: var(--r-muted);
    margin: 0 0 1.1rem;
  }

  .contact-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem 1.1rem;
    font-family: var(--r-mono);
    font-size: 0.82rem;
    margin-bottom: 1.5rem;
  }
  .contact-item { display: inline-flex; align-items: center; gap: 0.5rem; color: var(--r-ink); }
  .contact-item a { color: var(--r-ink); text-decoration: none; }
  .contact-item a:hover { color: var(--r-teal); text-decoration: underline; }
  .diamond { color: var(--r-teal); font-size: 0.6rem; line-height: 1; }

  .summary {
    font-family: var(--r-serif);
    font-size: 1.08rem;
    line-height: 1.7;
    color: var(--r-ink);
    max-width: 760px;
    margin: 0;
  }
  .summary :global(strong) { font-weight: 700; }

  /* Shared emphasis */
  .resume-sheet :global(strong) { color: var(--r-ink); font-weight: 700; }
  .resume-sheet :global(.metric) {
    font-family: var(--r-mono);
    font-weight: 700;
    font-size: 0.92em;
    color: var(--r-amber);
  }

  /* Grid */
  .resume-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 320px;
    gap: 3.5rem;
    margin-top: 2.5rem;
    padding-top: 2.5rem;
    border-top: 1px solid var(--r-border);
    position: relative;
  }
  .resume-aside {
    border-left: 1px solid var(--r-border);
    padding-left: 2.25rem;
  }

  /* Section heads */
  .sec-head {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.75rem;
  }
  .sec-head h2 {
    font-family: var(--r-mono);
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--r-teal);
    margin: 0;
    white-space: nowrap;
  }
  .sec-head::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--r-teal-line);
  }

  /* Experience */
  .exp-item {
    display: grid;
    grid-template-columns: 52px 1fr;
    gap: 1.1rem;
    padding-bottom: 1.6rem;
    margin-bottom: 1.6rem;
    border-bottom: 1px dashed var(--r-dash);
  }
  .exp-item:last-child { border-bottom: none; margin-bottom: 0; }
  .exp-item.compact { padding-bottom: 1.1rem; margin-bottom: 1.1rem; }

  .exp-logo {
    width: 52px;
    height: 52px;
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--r-slab);
    font-weight: 700;
    font-size: 1.4rem;
    color: #f7f5ef;
    flex-shrink: 0;
  }
  .exp-logo.teal { background: var(--r-teal); }
  .exp-logo.dark { background: #34322d; }
  html.dark .exp-logo.dark { background: #45433d; }

  .exp-body { min-width: 0; }
  .exp-top {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .exp-title {
    font-family: var(--r-serif);
    font-weight: 700;
    font-size: 1.25rem;
    color: var(--r-ink);
    margin: 0;
    line-height: 1.2;
  }
  .exp-title.small { font-size: 1.1rem; }
  .exp-dates {
    font-family: var(--r-mono);
    font-size: 0.78rem;
    color: var(--r-muted);
    white-space: nowrap;
  }
  .exp-meta {
    font-family: var(--r-mono);
    font-size: 0.78rem;
    color: var(--r-teal);
    margin: 0.3rem 0 0;
  }
  .mid-dot { color: var(--r-muted); margin: 0 0.45rem; opacity: 0.7; }

  .exp-context {
    font-family: var(--r-serif);
    font-style: italic;
    font-size: 0.92rem;
    color: var(--r-muted);
    margin: 0.55rem 0 0.65rem;
  }

  .exp-bullets {
    margin: 0.65rem 0 0;
    padding: 0;
    list-style: none;
  }
  .exp-bullets li {
    position: relative;
    padding-left: 1.25rem;
    font-family: var(--r-serif);
    font-size: 0.98rem;
    line-height: 1.6;
    color: var(--r-ink);
    margin-bottom: 0.45rem;
    max-width: 760px;
  }
  .exp-bullets li::before {
    content: '◆';
    position: absolute;
    left: 0;
    top: 0.05em;
    font-size: 0.55rem;
    color: var(--r-teal);
  }

  .exp-gov {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem;
    margin: 0.85rem 0 0;
  }
  .gov-label {
    font-family: var(--r-mono);
    font-size: 0.68rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--r-muted);
    margin-right: 0.25rem;
  }
  .gov-chip {
    font-family: var(--r-mono);
    font-size: 0.68rem;
    padding: 0.18rem 0.5rem;
    border: 1px solid var(--r-teal-line);
    border-radius: 5px;
    color: var(--r-teal);
    background: var(--r-teal-soft);
  }

  /* Aside */
  .aside-block { margin-bottom: 2rem; }
  .aside-head {
    font-family: var(--r-mono);
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--r-teal);
    margin: 0 0 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--r-teal-line);
  }

  .pill-cloud { display: flex; flex-wrap: wrap; gap: 0.45rem; }
  .pill {
    font-family: var(--r-mono);
    font-size: 0.72rem;
    padding: 0.32rem 0.6rem;
    border: 1px solid var(--r-border);
    border-radius: 6px;
    background: var(--r-surface);
    color: var(--r-ink);
  }
  .pill.core {
    background: var(--r-teal-soft);
    border-color: var(--r-teal-line);
    color: var(--r-teal);
  }

  .edu-item { margin-bottom: 1.1rem; }
  .edu-item:last-child { margin-bottom: 0; }
  .edu-degree {
    font-family: var(--r-serif);
    font-weight: 700;
    font-size: 1rem;
    color: var(--r-ink);
    margin: 0 0 0.2rem;
  }
  .edu-meta {
    font-family: var(--r-mono);
    font-size: 0.76rem;
    color: var(--r-muted);
    margin: 0;
  }
  .edu-meta.light { opacity: 0.8; margin-top: 0.15rem; }

  .cert-list { list-style: none; margin: 0; padding: 0; }
  .cert-list li {
    display: flex;
    align-items: baseline;
    gap: 0.55rem;
    font-family: var(--r-mono);
    font-size: 0.76rem;
    line-height: 1.4;
    color: var(--r-ink);
    margin-bottom: 0.6rem;
  }
  .cert-body { display: flex; flex-direction: column; gap: 0.15rem; }
  .cert-note {
    font-size: 0.68rem;
    color: var(--r-muted);
    opacity: 0.85;
  }

  /* Specializations */
  .spec-section {
    margin-top: 3rem;
    padding-top: 2.5rem;
    border-top: 1px solid var(--r-border);
  }
  .spec-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 0.85rem;
  }
  .spec-card {
    background: var(--r-surface);
    border: 1px solid var(--r-border);
    border-left: 3px solid var(--r-teal);
    border-radius: 8px;
    padding: 0.85rem 1rem;
  }
  .spec-title {
    font-family: var(--r-serif);
    font-weight: 700;
    font-size: 0.95rem;
    color: var(--r-ink);
    margin: 0 0 0.2rem;
    line-height: 1.25;
  }
  .spec-org {
    font-family: var(--r-mono);
    font-size: 0.74rem;
    color: var(--r-muted);
    margin: 0;
  }

  /* Footer */
  .resume-footer {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid var(--r-border);
  }
  .resume-actions { display: flex; justify-content: center; gap: 1rem; }
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 1.4rem;
    border-radius: 30px;
    font-family: var(--r-mono);
    font-size: 0.82rem;
    font-weight: 500;
    letter-spacing: 0.03em;
    text-decoration: none;
    cursor: pointer;
    border: none;
    transition: all 0.25s ease;
  }
  .btn-primary { background: var(--r-teal); color: #f7f5ef; }
  .btn-primary:hover { transform: translateY(-2px); filter: brightness(1.08); }
  .btn-secondary { background: transparent; color: var(--r-teal); border: 1.5px solid var(--r-teal); }
  .btn-secondary:hover { background: var(--r-teal); color: #f7f5ef; transform: translateY(-2px); }

  /* Print */
  @media print {
    .resume-sheet { margin: 0; background: #ffffff; }
    .back-link, .resume-footer { display: none; }
    .resume-shell { padding: 1rem 1.5rem; max-width: 100%; }
    .spec-section, .resume-grid { page-break-inside: auto; }
    .exp-item { page-break-inside: avoid; }
  }

  /* Responsive */
  @media (max-width: 900px) {
    .resume-grid { grid-template-columns: 1fr; gap: 2.5rem; }
    .resume-aside { border-left: none; padding-left: 0; border-top: 1px solid var(--r-border); padding-top: 2rem; }
  }

  /* Below the side-nav breakpoint the Layout removes the 240px offset,
     so the sheet should bleed to the full viewport instead. */
  @media (max-width: 768px) {
    .resume-sheet { width: calc(100% + 4rem); }
  }

  @media (max-width: 640px) {
    .resume-shell { padding: 1.5rem 1.25rem 2.5rem; }
    .exp-item { grid-template-columns: 42px 1fr; gap: 0.85rem; }
    .exp-logo { width: 42px; height: 42px; border-radius: 11px; font-size: 1.1rem; }
    .exp-top { gap: 0.25rem; }
    .summary { font-size: 1rem; }
  }
</style>
